sudo: false

language: python
python:
  - "3.5"

addons:
  apt:
    packages:
      - libxcursor-dev
      - libxinerama-dev
      - libxrandr-dev

install: pip install tox-travis
script: ./run_tests.py
